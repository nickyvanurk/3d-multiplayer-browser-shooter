# Absolute base URL of the game server (WebSocket + server-hosted music). Baked
# into every production build so the client connects back here no matter where
# the static bundle is hosted — nickvanurk.com/voidfall or CrazyGames' CDN. See
# client/src/config.ts.
VITE_ASSET_BASE_URL=https://nickvanurk.com/voidfall
